# Mini Blog Python 依赖
# 安装：pip install -r requirements.txt

Django==4.2.30          # Web 框架（SAD §3.2）
Markdown==3.10.2        # 文章正文 Markdown 渲染
bleach==6.4.0           # XSS 过滤（剥离危险 HTML 标签）

# 以下为 Django 的间接依赖（自动安装，列出以固定版本）
asgiref==3.11.1
sqlparse==0.5.5
webencodings==0.5.1

# 以下为 MySQL 后端所需（生产/验收部署时启用），SQLite 后端无需安装：
# mysqlclient==2.2.4    # MySQL 数据库驱动（需系统先装 libmysqlclient-dev）
#
# 如无法安装 mysqlclient，可改用纯 Python 驱动：
# PyMySQL==1.1.1        # 并在 miniblog/__init__.py 中加 pymysql.install_as_MySQLdb()

# 头像上传所需
Pillow==10.2.0
